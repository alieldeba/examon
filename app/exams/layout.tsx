export const metadata = {
  title: "Examon | Exams",
  description:
    "Discover our exams and try your best to solve them and gain more points to level up your account.",
};

export default function LeaderboardLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
